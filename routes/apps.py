from django.apps import AppConfig


class RoutesConfig(AppConfig):
    """This class is used to define the routes config class."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "routes"
    verbose_name = "Gestione Percorsi"
